Installation
------------

To install, run

    git clone https://shenbin04@bitbucket.org/shenbin04/zsh-config.git ~/.zsh
    cd ~/.zsh
    make install

To update the repository, run

    make update
